#include <data_structures/doubly_linked_list_node.hpp>

namespace data_structures {
    DoublyLinkedListNode::DoublyLinkedListNode(const int data)
        : next(nullptr),
          data_(data) {
    }
}
